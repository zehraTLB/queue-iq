export const CLINIC = {
  openMinutes: 8 * 60,
  closeMinutes: 18 * 60,
  slotGranularity: 15,
  minGapMinutes: 15,
  lunchStart: 13 * 60,
  lunchEnd: 13 * 60 + 30,
};

export const OVERBOOK_RISK_THRESHOLD = 50;
export const OVERBOOK_MAX_COLLISION = 0.4;
